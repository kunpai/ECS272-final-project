import React, { useEffect, useRef } from 'react';
import * as d3 from 'd3';
import { createPortal } from 'react-dom';

const StarChart = ({ parameter, view }) => {
    console.log(parameter);
    console.log(view);
}

export default StarChart;